apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namePrefix: prd-
commonLabels:
  app: test
bases:
- ../../base

resources:
- namespace.yaml
patchesStrategicMerge:
- deployment_patch.yaml